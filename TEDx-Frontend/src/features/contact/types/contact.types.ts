export interface ContactPayload {
 name: string;
  email: string;
  subject: string;
  message: string;
  
  // TODO: Uncomment when backend supports these fields
  // department?: string;
  // phone?: string;
}